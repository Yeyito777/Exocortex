{
  "name": "image",
  "bin": "./bin/image",
  "systemHint": "You can generate raster images through the `image` CLI tool. Run `image -h` for usage reference. Use `image generate <prompt>` to create assets; it prints the saved image path.",
  "display": {
    "label": "Image",
    "color": "#ffb86c"
  },
  "shell": {
    "literalArgs": [
      { "subcommand": "generate", "kind": "tail" }
    ]
  },
  "auth": {
    "providers": ["openai"]
  }
}
